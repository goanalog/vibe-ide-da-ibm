
// Function URL is available in Terraform outputs as `vibe_push_function_url`.
// Paste it here to enable push-live from the browser (or render via Terraform template if desired).
window.VIBE_CONFIG = {
  FUNCTION_URL: "" // e.g., "https://us-south.functions.appdomain.cloud/api/v1/web/<ns>/default/vibe-push"
};
