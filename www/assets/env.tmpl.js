// Terraform will template these values during deployment.
window.__VIBE_ENV__ = {
  APPID_CLIENT_ID: "${APPID_CLIENT_ID}",
  APPID_TENANT_ID: "${APPID_TENANT_ID}",
  APP_REGION: "${APP_REGION}",
  PUSH_API_URL: "${PUSH_API_URL}"
};